[project]
name = "hexlet-code"
version = "0.1.0"
description = "page-analyzer"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.2",
]
